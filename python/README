The scripts for analyzing the data. 

All helper functions live in the helpers file.

All other scripts can be run to produce results, run with -h to see available options and what the script does.

builddb : This builds the database used by the other scripts. 

readdb  : An example script to read the database. Can print
          all results for all constituencies, or the result
          for a specific constituency.
