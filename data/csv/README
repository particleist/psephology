The data in csv format which python can actually read.

The 2015.csv data is in a shortened format which doesn't include the northern ireland results in particular.
The 2015-corrected.csv file does include those results and can be used to build a database with them
if so desired.
